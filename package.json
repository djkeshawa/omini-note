{
  "name": "vispnote",
  "version": "0.1.20",
  "description": "VispNote — a local-first, block-based note-taking desktop app",
  "license": "Apache-2.0",
  "author": {
    "name": "djkeshawa",
    "url": "https://github.com/djkeshawa"
  },
  "files": [
    "vispnote.html",
    "main.js",
    "preload.js",
    "assets/",
    "build/renderer/",
    "lib/",
    "src/",
    "scripts/linux-after-install.sh",
    "scripts/build-renderer.js",
    "scripts/verify-packaged-renderer.js",
    "electron-builder.yml"
  ],
  "homepage": "https://github.com/djkeshawa/visp-note#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/djkeshawa/visp-note.git"
  },
  "main": "main.js",
  "engines": {
    "node": ">=24.15.0 <25",
    "npm": ">=11"
  },
  "scripts": {
    "build:renderer": "node scripts/build-renderer.js",
    "verify:package-renderer": "node scripts/verify-packaged-renderer.js",
    "check:main": "npm run lint -- --quiet=false && node --check main.js && node --check preload.js && node --check lib/store.js && node --check lib/index.js && node --check lib/ai.js && node --check lib/ollama.js",
    "test:editor": "node --test tests/*.test.js",
    "smoke:electron": "npm run build:renderer && electron --no-sandbox scripts/smoke-electron.js",
    "prebuild": "npm run build:renderer",
    "build": "electron-builder --publish never",
    "prebuild:mac": "npm run build:renderer",
    "build:mac": "electron-builder --mac --publish never",
    "prebuild:linux": "npm run build:renderer",
    "build:linux": "electron-builder --linux --publish never",
    "prebuild:win": "npm run build:renderer",
    "build:win": "electron-builder --win nsis --publish never",
    "prebuild:win:portable": "npm run build:renderer",
    "build:win:portable": "electron-builder --win portable --publish never",
    "start": "npm run build:renderer && electron .",
    "postinstall": "electron-builder install-app-deps",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "electron": "^41.3.0",
    "electron-builder": "^26.8.1",
    "esbuild": "^0.28.0",
    "eslint": "^10.3.0",
    "globals": "^17.6.0"
  },
  "dependencies": {
    "better-sqlite3": "^12.9.0",
    "electron-updater": "^6.8.3",
    "katex": "^0.16.45",
    "mermaid": "^11.14.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "sqlite-vec": "^0.1.9"
  }
}
